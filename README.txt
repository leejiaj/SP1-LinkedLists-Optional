CS 5V81 SP1 (optional; individual): Linked Lists

Leejia James

How to Compile:
1. Unzip contents of lxj171130.zip into a folder named "lxj171130".
2. Run the command `javac lxj171130/SinglyLinkedList.java` to compile.
3. Run the command `java lxj171130.SinglyLinkedList` to execute the SinglyLinkedList.


Implemented below:
--------------------
Optional Task2: Added the methods addFirst(x), removeFirst(), remove(x).
Optional Task3: Added indexing operations: get(index), set(index, x), add(index, x), remove(index).
Optional Task4: Added separate options to test operations add(x) and unzip() 
				(These are not errors per se)